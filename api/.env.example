PORT=3000

# Copy this file to .env and replace the placeholder values with your real secrets.
# Authentication and database credentials must never be committed to version control.

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/blog_api?schema=public"
JWT_SECRET=change-me
